<Window x:Class="BackupUtility.Views.BackupTimeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BackupUtility.Views"
        xmlns:vm="clr-namespace:BackupUtility.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:BackupTimeWindowViewModel}"
        WindowStyle="None" WindowStartupLocation="CenterScreen"
        Title="Backup Time"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Background="Transparent">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}" />
    </WindowChrome.WindowChrome>
    <Border Background="{DynamicResource Window.Background}" BorderBrush="{DynamicResource Window.Border}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid x:Name="CaptionBar" Margin="4,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="46"/>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Width="18" Height="18" Source="/Backup.ico" />
                <TextBlock Grid.Column="1" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="14"
                       Foreground="{DynamicResource CaptionBar.Foreground}"
                       Text="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Title}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActive, RelativeSource={RelativeSource AncestorType=Window}}" Value="False">
                                    <Setter Property="Foreground" Value="#818181" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <Button Grid.Column="2" Style="{DynamicResource CaptionBar.CloseButton}" Command="{Binding CancelCommand}">
                    <Path Width="12" Height="12" Data="M 2,2 l8,8 M 10,2 l-8,8"
                      Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                      StrokeThickness="1" />
                </Button>
            </Grid>
            <Grid Grid.Row="1" HorizontalAlignment="Center" Margin="15,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0"
                          FontSize="16"
                          ItemsSource="{Binding Hours}"
                          SelectedItem="{Binding SelectedHour, Mode=TwoWay}"/>
                <TextBlock Grid.Column="1" Text=":"
                           VerticalAlignment="Center" HorizontalAlignment="Center"
                           FontWeight="Bold" FontSize="24" Margin="0,0,0,8"
                           Foreground="{DynamicResource Window.Foreground}"/>
                <ComboBox Grid.Column="2"
                          FontSize="16"
                          ItemsSource="{Binding Minutes}"
                          SelectedItem="{Binding SelectedMinute, Mode=TwoWay}"/>
                <ComboBox Grid.Column="3"
                          FontSize="16"
                          ItemsSource="{Binding Meridiems}"
                          SelectedItem="{Binding SelectedMeridiem, Mode=TwoWay}"
                          Margin="24,0,0,0"/>
            </Grid>
            <Button x:Name="SaveButton" 
                    Grid.Row="3"
                    HorizontalAlignment="Right" 
                    Content="Save"
                    Width="74"
                    Margin="8"
                    Command="{Binding OkCommand}"/>
        </Grid>
    </Border>
</Window>
